Create an `animal_fact_profiles` record from the following raw animal source record.

RAW ANIMAL RECORD (JSON)
{RAW_ANIMAL_JSON}

Instructions:
- Populate all schema fields as completely as the source allows.
- `dog_name`: Must use proper case and should not end in an asterisk. If the root `name` field is missing, infer it from the record text.
- `age_summary`: Format exactly as "x yr, y mo", "x yr", or "y mo". If given in decimals, convert (e.g. 1.5yr -> "1 yr, 6 mo"). Round if needed. If no age info, use "N/A".
- `age_bucket`: Infer from `age_summary` into exactly one of: "Puppy (<1yr)", "Young (1-3yrs)", "Adult (3-7yrs)", "Senior (8+yrs)", or "N/A".
- `weight_summary`: Format exactly as "x lbs" with no decimal points. No other words like "Approximately". If no info, use "N/A".
- `weight_class`: Infer from `weight_summary` into exactly one of: "Small (<25lbs)", "Medium (25-60lbs)", "Large (60+lbs)", or "N/A".
- `breed_or_description`: Extract breed/visual description from anywhere in the record (often in bio). Use proper casing. Remove dashes if extracting from a url. Include words like "Mix" if used. If cannot be inferred, use "Unknown". DO NOT include any color information.
- `sex`: "Male", "Female", or "N/A". If the exact sex is not provided, look for hints in the bio (e.g. use of "He/His" implies Male, "She/Her" implies Female).
- `altered_status`: "Spayed", "Neutered", "Unaltered", or "N/A".
- `location_detail`: Specific location info beyond the shelter (e.g., NYC borough, foster home, kennel number, room number).
- `important_facts_jsonb`, `risk_flags_jsonb`, `unknowns_jsonb`, `strengths_jsonb`, `challenges_jsonb`, `ideal_home_jsonb`, and `management_notes_jsonb` should be arrays of short strings.
- Keep `backstory_summary` concise and factual.
- `other_animals_notes`, `people_notes`, `containment_notes`, `medical_notes`, and `adoption_process_notes` should be plain-language summaries.
- Evidence snippets should cite short phrases from the input text, not external data.
- If something is truly absent, do not guess (use "N/A" for strings if instructed, else leave empty/unknown).

- `energy_level`: Infer from behavioral descriptions, activity levels, and temperament notes. Use exactly one of: "calm" (relaxed, mellow, couch potato, low-key, gentle, easygoing), "high" (energetic, playful, needs lots of exercise, hyperactive, active, loves to run), or "N/A" if no energy-related info is available. Do NOT guess — only infer from explicit behavioral descriptions.
- `good_with_dogs`: "yes" ONLY if the source explicitly says the dog is good with, friendly with, or lives well with other dogs. "no" if the source says the dog is not good with dogs, dog-reactive, or dog-selective. "unknown" if not mentioned or ambiguous. Do NOT infer from general friendliness.
- `house_trained`: "yes" ONLY if the source explicitly says house trained, potty trained, or housebroken. "no" if explicitly stated the dog is not house trained. "unknown" if not mentioned. Do NOT infer from age or other context.

- `sugg_specific`: Generate up to 5 short, clickbait-style suggested prompts that are specifically tailored to unique or interesting details found in this dog's profile/bio. These are leading questions designed like conversation hooks — things a potential adopter might be curious about after reading the bio. They should be answerable using information available in the dog's record.
  Rules for sugg_specific:
  - Keep them SHORT (under 10 words each is ideal) to conserve display space.
  - They can be fun/quirky (e.g., "What do you have against the garbage truck?") or serious/informative (e.g., "Tell me about your eye condition").
  - They must reference SPECIFIC details from this dog's profile — not generic questions.
  - If the bio is very short or lacks interesting details, produce fewer than 5. Quality over quantity. Never invent details or make up unanswerable questions.
  - ALWAYS phrase in 2nd person, as if the user is speaking directly to the dog. Use "you/your" instead of the dog's name. Example: "What commands do you know?" NOT "What commands does Jax know?"
  - They must NOT duplicate or closely overlap with ANY of the following Informative category questions:
    * "What's your rescue story?"
    * "Are you good with other dogs or cats?"
    * "Are you good with kids or strangers?"
    * "Do you have any medical needs?"
    * "What should my human know?"
    * "What makes you unique?"
    * "What are your strengths?"
    * "What does your ideal home look like?"
    * "What is your energy level?"
    * "What is your adoption process?"

Return only valid JSON matching the schema.
